<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Attestia.App.Pages.ReconciliationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Loaded="Page_Loaded">

    <ScrollViewer>
        <StackPanel Padding="24" Spacing="16">
            <!-- Header -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Reconciliation" FontSize="28" FontWeight="SemiBold" />
                <Button Grid.Column="1" Content="Refresh" Click="Refresh_Click" />
            </Grid>

            <!-- Attestation list -->
            <StackPanel Spacing="8">
                <TextBlock Text="Attestation Records" FontSize="18" FontWeight="SemiBold" />
                <ListView x:Name="AttestationsList" SelectionMode="Single"
                          SelectionChanged="Attestation_Selected"
                          ItemsSource="{x:Bind ViewModel.Attestations, Mode=OneWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="12" Margin="0,0,0,4"
                                  CornerRadius="6"
                                  Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                  BorderThickness="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0"
                                           Text="{Binding Id}" FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis" />
                                <Border Grid.Row="0" Grid.Column="1" Padding="6,2" CornerRadius="4"
                                        Background="{ThemeResource SubtleFillColorSecondaryBrush}">
                                    <TextBlock Text="{Binding AllReconciled}" FontSize="11" />
                                </Border>

                                <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal"
                                            Spacing="12" Margin="0,4,0,0">
                                    <TextBlock FontSize="11"
                                               Foreground="{ThemeResource TextFillColorTertiaryBrush}">
                                        <Run Text="Attested: " />
                                        <Run Text="{Binding AttestedAt}" />
                                    </TextBlock>
                                    <TextBlock FontSize="11" FontFamily="Consolas"
                                               TextTrimming="CharacterEllipsis" MaxWidth="300"
                                               Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                               Text="{Binding ReportHash}" />
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>

            <!-- Summary panel (shown when attestation selected) -->
            <StackPanel x:Name="SummaryPanel" Visibility="Collapsed" Spacing="8">
                <TextBlock Text="Summary" FontSize="18" FontWeight="SemiBold" />
                <Grid Padding="16" CornerRadius="8"
                      Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                      BorderThickness="1" ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Spacing="4">
                        <TextBlock Text="Intents" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="SumIntents" Text="—" FontSize="24" FontWeight="Bold" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Spacing="4">
                        <TextBlock Text="Matched" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="SumMatched" Text="—" FontSize="24" FontWeight="Bold" />
                    </StackPanel>

                    <StackPanel Grid.Column="2" Spacing="4">
                        <TextBlock Text="Mismatched" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="SumMismatched" Text="—" FontSize="24" FontWeight="Bold" />
                    </StackPanel>

                    <StackPanel Grid.Column="3" Spacing="4">
                        <TextBlock Text="Missing" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="SumMissing" Text="—" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Grid>

                <!-- Discrepancies -->
                <StackPanel x:Name="DiscrepanciesPanel" Visibility="Collapsed" Spacing="4">
                    <TextBlock Text="Discrepancies" FontSize="14" FontWeight="SemiBold"
                               Foreground="Orange" />
                    <ItemsControl x:Name="DiscrepanciesList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" FontSize="12"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                           Margin="0,2,0,0" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </StackPanel>

            <!-- Loading / Error -->
            <StackPanel Orientation="Horizontal" Spacing="8">
                <ProgressRing x:Name="LoadingRing" IsActive="False" Width="20" Height="20" />
                <TextBlock x:Name="ErrorText" Foreground="Red" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
